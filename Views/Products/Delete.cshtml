@model OrderAutomation.Models.Product

@{
    ViewData["Title"] = "Удаление продукта";
}

<h1>Удаление продукта</h1>

<h3>Вы уверены, что хотите удалить этот продукт?</h3>
<div>
    <hr />
    <div class="row">
        <div class="col-md-8">
            <dl class="row">
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Name)
                </dd>
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.SKU)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.SKU)
                </dd>
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Description)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Description)
                </dd>
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Price)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Price)
                </dd>
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.StockQuantity)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.StockQuantity)
                </dd>
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.Category)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Category.Name)
                </dd>
            </dl>
        </div>
    </div>
    
    @if ((Model.ProductReceipts != null && Model.ProductReceipts.Any()) || (Model.OrderItems != null && Model.OrderItems.Any()))
    {
        <div class="alert alert-warning mt-3">
            <h5>Внимание!</h5>
            @if (Model.ProductReceipts != null && Model.ProductReceipts.Any())
            {
                <p>Этот продукт имеет связанные записи о поступлениях (@Model.ProductReceipts.Count). При удалении продукта все записи о поступлениях будут удалены.</p>
            }
            @if (Model.OrderItems != null && Model.OrderItems.Any())
            {
                <p>Этот продукт используется в @Model.OrderItems.Count заказах. При удалении продукта все позиции в заказах, содержащие этот продукт, будут удалены.</p>
            }
        </div>
    }
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-outline-secondary">Вернуться к списку</a>
    </form>
</div> 